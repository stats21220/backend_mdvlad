name: Publish Docker

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Publish to registy
        uses: elgohr/Publish-Docker-Github-Action@master
        with:
          registry: docker.pkg.github.com
          name: docker.pkg.github.com/stats21220/backend_mdvlad/mdvlad
          username: ${{ secrets.actions.DOCKER_USERNAME }}
          password: ${{ secrets.actions.DOCKER_PASSWORD }}
          tags: 'develop'
